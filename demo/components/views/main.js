'use strict';

import React from 'react';
import icon from '../../icon.ico';

const Main = () => (
  <div>
    TODO
  </div>
);

Main.meta = {
  exclude: true,
  icon,
  title: 'Main'
};

export default Main;
